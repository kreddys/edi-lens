# ==============================================================================
# EDI Lens Environment Configuration - LOCAL DEVELOPMENT
# ==============================================================================
# This file is used when running tests and development locally.
# Services run in Docker containers, but backend/tests connect via localhost URLs.
# Based on .env.example - only contains overrides for local development.
# ==============================================================================

# DATABASE CONFIGURATION - Local connection to Docker service
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/edi_lens
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=edi_lens
POSTGRES_MULTIPLE_DATABASES=nifi_registry

# NIFI CONFIGURATION - Local connection to Docker service
NIFI_URL=https://localhost:8443
NIFI_USERNAME=admin
NIFI_PASSWORD=adminadmin123
NIFI_SENSITIVE_PROPS_KEY=simple-key-for-development
NIFI_JVM_HEAP_INIT=1g
NIFI_JVM_HEAP_MAX=2g
NIFI_WEB_HTTPS_PORT=8443
NIFI_WEB_HTTPS_HOST=0.0.0.0

# NIFI REGISTRY CONFIGURATION - Local connection to Docker service
NIFI_REGISTRY_URL=http://localhost:18080
NIFI_REGISTRY_AUTH_TOKEN=
NIFI_REGISTRY_WEB_HTTP_HOST=0.0.0.0
NIFI_REGISTRY_WEB_HTTP_PORT=18080
NIFI_REGISTRY_DB_URL=jdbc:postgresql://db:5432/nifi_registry
NIFI_REGISTRY_DB_USER=postgres
NIFI_REGISTRY_DB_PASS=postgres
NIFI_REGISTRY_DB_CLASS=org.postgresql.Driver
NIFI_REGISTRY_DB_DRIVER_CLASS=org.postgresql.Driver
NIFI_REGISTRY_DB_DRIVER_DIRECTORY=/opt/nifi-registry/nifi-registry-current/lib

# BACKEND APPLICATION CONFIGURATION
APP_VERSION=0.1.0
DEBUG=true
NIFI_VERIFY_SSL=false
REGISTRY_VERIFY_SSL=false

# LOGGING CONFIGURATION - Optimized for local development
LOG_LEVEL_CONSOLE=INFO
LOG_LEVEL_CLIENTS=INFO
LOG_LEVEL_SERVICES=INFO
LOG_LEVEL_API=INFO
