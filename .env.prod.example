# This is a template for the PRODUCTION environment.
# The contents of this file should be pasted into the DOT_ENV GitHub secret.

# --- Production URLs ---
REMOTE_HOST=your-domain.com

# --- PostgreSQL Configuration ---
POSTGRES_USER=edi_user
POSTGRES_PASSWORD=a-very-strong-production-password
POSTGRES_DB=edi_lens
POSTGRES_SERVER=db
POSTGRES_PORT=5432

# --- Backend API Configuration ---
LOG_LEVEL=INFO
BACKEND_HOST=backend
EDI_SCHEMA_DIRECTORY=/var/edi_lens/schemas

# --- Keycloak Configuration ---
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=another-strong-production-password

# --- Configurable User Passwords ---
KC_SUPERUSER_PASSWORD=a-strong-superuser-pass
KC_ADMIN_A_PASSWORD=a-strong-admin-a-pass
KC_VIEWER_B_PASSWORD=a-strong-viewer-b-pass

KEYCLOAK_DB_VENDOR=postgres
KEYCLOAK_DB_HOST=db
KEYCLOAK_DB_PORT=5432
KEYCLOAK_DB_DATABASE=${POSTGRES_DB}
KEYCLOAK_DB_USER=${POSTGRES_USER}
KEYCLOAK_DB_PASSWORD=${POSTGRES_PASSWORD}
KEYCLOAK_DB_SCHEMA=keycloak

KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_BROWSER_URL=https://auth.${REMOTE_HOST}
KEYCLOAK_REALM=edi-lens
KEYCLOAK_BACKEND_CLIENT_ID=edi-lens-backend
KEYCLOAK_BACKEND_CLIENT_SECRET=a-different-very-secret-key-for-prod
KEYCLOAK_UI_CLIENT_ID=edi-lens-ui

# This tells Keycloak which origins can embed it in an iframe.
# The entire value MUST be enclosed in double quotes.
KC_SPI_FRAME_ANCESTORS="'self' https://${REMOTE_HOST}"

# --- Admin UI / Vite Configuration ---
VITE_API_URL=https://api.${REMOTE_HOST}/api/v1
VITE_KEYCLOAK_URL=${KEYCLOAK_BROWSER_URL}
VITE_KEYCLOAK_REALM=${KEYCLOAK_REALM}
VITE_KEYCLOAK_CLIENT_ID=${KEYCLOAK_UI_CLIENT_ID}